# TMDB API read access token (v4 bearer) — https://www.themoviedb.org/settings/api
# Only needed to run `npm run refresh`. The app itself never calls TMDB from the browser.
TMDB_TOKEN=

# Optional: comma-separated ISO-3166-1 regions to build watch-provider data for.
REGIONS=IN,US
